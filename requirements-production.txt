# Python dependencies for RDFAnalyzerCore Production Manager
#
# Core dependencies (required)
pyyaml>=5.4

# For DASK backend (optional)
# Uncomment to enable DASK distributed computing support
# dask>=2021.0.0
# distributed>=2021.0.0
# dask-jobqueue>=0.7.3

# For LAW integration (optional, future)
# Uncomment to enable Luigi Analysis Workflow integration
# law>=0.1.0

# Note: Other dependencies are either:
# - Provided by ROOT/CMSSW environment (e.g., numpy, requests)
# - Part of Python standard library (e.g., json, pathlib, subprocess)
# - Optional and detected at runtime (e.g., curses, rucio)
